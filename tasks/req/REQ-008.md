---
req_id: REQ-008
title: "Agent UID 注册与角色解耦"
status: tc_review
owner: claude
priority: P1
phase: PHASE-000
scope: harness
tc_policy: required
tc_exempt_reason: ""
depends_on: [REQ-001, REQ-007]
test_case_ref: [TC-008-01, TC-008-02, TC-008-03, TC-008-04, TC-008-05, TC-008-06, TC-008-07, TC-008-08, TC-008-09, TC-008-10, TC-008-11]
acceptance: "AGENT_UID=optimizer-001 bash scripts/claim-req.sh REQ-NNN 退出码 0；bash scripts/claim-req.sh REQ-NNN optimizer-001 退出码 0；AGENT_REGISTRY=<fixture> AGENT_UID=<fixture-uid> bash scripts/claim-req.sh REQ-NNN 退出码 0（证明注册表动态读取）；bash scripts/check-req-coverage.sh 在 owner 为有效 UID 时通过、在 owner 为 claude/codex/daniel 旧格式时报错"
review_round: 0
pending_bugs: []
blocked_reason: ""
blocked_from_status: ""
blocked_from_owner: ""
pr_number: ""
---

# Goal

将需求状态机中的 `owner` 字段从硬编码模型名（`claude`、`codex`、`daniel`）替换为角色 UID（`optimizer-001`、`evaluator-001`、`human-001`），并引入 `harness/agent-registry.yml` 作为唯一注册表。

这一变更体现了 Anthropic Harness 实践中 Optimizer/Evaluator 角色与具体模型实例的解耦：今天 optimizer-001 是 Claude claude-sonnet-4-6，未来可以替换为任何其他模型，而 REQ 文件和状态机逻辑不需要变更。

# Background

当前 `owner` 字段直接写模型名（`claude`/`codex`），绑定了角色与实现。如果：
- 引入第二个 optimizer（并行开发两个 REQ）→ 无法区分
- 切换到不同 LLM 提供商的 optimizer → 必须批量修改所有 REQ 文件
- 添加 evaluator 角色（如一个专门做 security review 的 evaluator-002）→ hardcoded 校验脚本无法扩展

UID 注册表解决这三个问题。

# In Scope

1. **`harness/agent-registry.yml`** — 角色注册表（source of truth）
   - 三条初始记录：`optimizer-001`（Claude）、`evaluator-001`（Codex）、`human-001`（Daniel）
   - 每条记录含：`uid`、`role`、`model`、`description`、`handles`（该角色有效工作的 REQ 状态列表）

2. **`.env.example`** — 新增 `AGENT_UID` 变量，预填 `optimizer-001`，注释说明各 agent 的取值

3. **`scripts/claim-req.sh`** — 重写校验逻辑
   - 接受 UID 格式：`bash scripts/claim-req.sh REQ-NNN`（从 `$AGENT_UID` 读取）
   - 或显式传入：`bash scripts/claim-req.sh REQ-NNN optimizer-001`（显式参数优先于 `$AGENT_UID`）
   - 注册表路径默认为 `harness/agent-registry.yml`；可通过 `AGENT_REGISTRY=<path>` 环境变量覆盖（供测试注入 fixture）
   - 从注册表动态读取该 UID 的 `handles` 列表，替代 hardcoded case 分支
   - 未知 UID（不在注册表中）→ HARD STOP

4. **`harness/req-constants.sh`** — valid owners 改为从注册表动态派生（所有已注册 UID + `unassigned`），不再硬编码模型名
   - 注册表路径同样遵循 `AGENT_REGISTRY` 环境变量（与 claim-req.sh 保持一致）

5. **`harness/requirement-standard.md`** — 更新 §3 frontmatter schema（`owner` 字段示例改为 UID 格式）和 §6 Agent Handoff Protocol（`claim: REQ-NNN by optimizer-001`）

6. **`harness/GLOSSARY.md §13`** — 更新 owner 枚举表，从模型名改为 UID 格式，说明注册表位置

7. **`CLAUDE.md`** — 新增"Agent Identity"节：说明本 Claude 实例注册为 `optimizer-001`，启动时读取 `.env` 的 `AGENT_UID`

8. **`AGENTS.md`** — 同步更新为 Codex 对应说明（`evaluator-001`）

9. **存量 REQ 迁移**（REQ-001 ~ REQ-007）
   - `owner: codex` → `owner: evaluator-001`
   - `owner: claude` → `owner: optimizer-001`
   - `owner: daniel` → `owner: human-001`
   - （`owner: unassigned` 不变）

# Out of Scope

- 运行时自动注册（agent 启动时写入注册表）— 静态注册表足够，不需要动态注册
- 多实例并行（optimizer-002）— 注册表格式已为此预留，但本 REQ 不创建第二个实例
- 自动化 inbox/handoff 系统 — 已在 CONNECTORS.md §2 推迟

# Acceptance Criteria

- AC1：`AGENT_UID=optimizer-001 bash scripts/claim-req.sh REQ-NNN` 在 REQ 处于 optimizer 有效状态时退出码 0
- AC2：`AGENT_UID=evaluator-001 bash scripts/claim-req.sh REQ-NNN` 在 REQ 处于 evaluator 有效状态时退出码 0
- AC3：`AGENT_UID=unknown-uid bash scripts/claim-req.sh REQ-NNN` 退出码 1，报错 "Unknown UID"
- AC4：`AGENT_UID=optimizer-001 bash scripts/claim-req.sh REQ-NNN`（REQ owner 为 `evaluator-001`）退出码 1，报错 owner 不匹配
- AC5：`bash scripts/check-req-coverage.sh` 在 REQ owner 为有效 UID 时通过
- AC6：`bash scripts/check-req-coverage.sh` 在 REQ owner 为旧格式（`claude`/`codex`/`daniel`）时报错，错误信息含 REQ ID 和字段名
- AC7：`harness/agent-registry.yml` 存在且包含 `optimizer-001`、`evaluator-001`、`human-001` 三条记录，每条含 `uid`、`role`、`model`、`handles` 字段
- AC8：`.env.example` 包含 `AGENT_UID=optimizer-001` 及注释
- AC9：所有 REQ-001 ~ REQ-007 的 `owner` 字段均为 UID 格式
- AC10：`bash scripts/claim-req.sh REQ-NNN optimizer-001`（不设 `$AGENT_UID`）在 REQ 处于 optimizer 有效状态时退出码 0（覆盖显式参数路径）
- AC11：`AGENT_REGISTRY=<fixture_registry> AGENT_UID=<fixture_uid> bash scripts/claim-req.sh REQ-NNN` 退出码 0，其中 fixture registry 只含该虚构 UID 及其 handles——证明 claim-req.sh 从注册表文件动态读取，而非内嵌 UID 表
- AC12：`AGENT_REGISTRY=<fixture_registry> bash scripts/check-req-coverage.sh`（测试 REQ owner 为 fixture UID）通过——证明 req-constants.sh 的 valid owner 列表同样从注册表动态派生，而非硬编码 optimizer-001/evaluator-001/human-001

# Test Case Design Notes

由 Codex 在 `tc_design` 阶段设计 TC 文件（`tasks/test-cases/TC-008-*.md`）：

- **TC-008-01**：claim-req.sh 正常路径（env var）— `AGENT_UID=optimizer-001` + REQ 处于 `req_impl` → 退出码 0
- **TC-008-02**：claim-req.sh 正常路径（env var）— `AGENT_UID=evaluator-001` + REQ 处于 `req_impl_review` → 退出码 0
- **TC-008-03**：claim-req.sh 未知 UID → 退出码 1，stderr 含 "Unknown UID"
- **TC-008-04**：claim-req.sh owner 不匹配 → 退出码 1，stderr 含 owner 值
- **TC-008-05**：claim-req.sh 旧格式 UID（`claude`）→ 退出码 1，旧格式不在注册表
- **TC-008-06**：check-req-coverage.sh 接受 UID 格式 owner → 通过
- **TC-008-07**：check-req-coverage.sh 拒绝旧格式 owner（`claude`/`codex`/`daniel`）→ 报错含 REQ ID 和字段名
- **TC-008-08**：registry 结构校验 — 所有必填字段存在，`handles` 列表非空
- **TC-008-09**：claim-req.sh 显式参数路径 — `bash scripts/claim-req.sh REQ-NNN optimizer-001`（不设 `$AGENT_UID`）+ REQ 处于 `req_impl` → 退出码 0（对应 AC10）
- **TC-008-10**：claim-req.sh 动态读取证明 — 创建 fixture registry（只含 `test-agent-999` 及其 handles），`AGENT_REGISTRY=<fixture> AGENT_UID=test-agent-999 bash scripts/claim-req.sh REQ-NNN`（REQ owner=test-agent-999，状态在 handles 内）→ 退出码 0；`test-agent-999` 不在主 registry，排除 hardcode 可能（对应 AC11）
- **TC-008-11**：req-constants.sh 动态读取证明 — 同一 fixture registry（只含 `test-agent-999`），创建临时 REQ stub（owner: test-agent-999，status: 任意有效值），`AGENT_REGISTRY=<fixture> bash scripts/check-req-coverage.sh` → 通过；再用主 registry 跑同一 REQ stub → 报错（`test-agent-999` 不是有效 owner）——双向验证 req-constants.sh 从 `AGENT_REGISTRY` 动态派生 valid owner 列表（对应 AC12）

注意：TC 均为 L1（shell 脚本单元测试），可用 `bats`（Bash Automated Testing System）或等效手段实现。如项目未引入 bats，可用 pytest 的 subprocess 调用。

# Agent Notes

- **`harness/agent-registry.yml` 解析**：`claim-req.sh` 和 `req-constants.sh` 用 `awk`/`grep` 解析 YAML，不引入额外依赖。`handles` 字段为空格分隔的状态列表（行内格式），脚本统一处理。注册表路径通过 `${AGENT_REGISTRY:-harness/agent-registry.yml}` 读取，允许测试注入 fixture。
- **原子迁移**：`req-constants.sh` 更新和 REQ-001~007 文件迁移必须在同一 commit 中，否则会出现 `check-req-coverage.sh` 在迁移过渡期报错的窗口。
- **bootstrap 例外**：REQ-008 本身在 `req_review` 阶段使用旧格式 `owner: codex`（因为 UID 系统尚未实施）。实施完成后，REQ-008 自身也作为迁移的一部分更新为 UID 格式。
- **向后兼容**：实施完成后，旧格式（`claude`/`codex`/`daniel`）成为 **无效** owner 值，`check-req-coverage.sh` 报错。不保留过渡期宽容窗口。

# Review History

> 完整 review 意见和 optimizer 回复见 PR #2 comment thread。此处只记各轮结论和 spec 变更摘要。

| Round | Verdict | 主要变更 |
|-------|---------|---------|
| 1 | Changes requested | 新增 `AGENT_REGISTRY` 覆盖机制（In Scope 3/4）；新增 AC10/TC-008-09（显式参数路径）；新增 AC11/TC-008-10（claim-req.sh registry 动态读取证明）；AC6 补 `daniel` |
| 2 | Changes requested | 新增 AC12/TC-008-11（req-constants.sh registry 动态读取证明，与 AC11 对称） |
| 3 | **Approved** (T03) | SUGGEST applied: TC ID 补全为 TC-008-NN 格式 |
